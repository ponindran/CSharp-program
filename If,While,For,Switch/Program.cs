using System;

namespace If_While_For_Switch
{
    class Program
    {
        static void Main(string[] args)
        {
            @if objname = new @if();
            objname.If();
            @switch week = new @switch();
            week.Switch();
            
            @while.While() ;

            @for num = new @for();
            num.For();
        }
    }
}